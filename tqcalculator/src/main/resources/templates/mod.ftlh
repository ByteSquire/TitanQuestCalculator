## Titan Quest Calculator

You can click any link shown below to get to the skill overview.

<#list masteries as mastery>
    [<#if mastery.name??>${mastery.name}<#else>name</#if>](${mastery.url})
</#list>